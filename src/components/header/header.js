import React, { Component } from 'react';
import Aux from '../../hoc/Aux';
import './header.scss';

class Header extends Component {
    render() {
        return (
            <Aux>
                <div>Welcome header</div>
            </Aux>
        )
    };
}

export default Header;